package com.playdata.calen.drive.repository;

import com.playdata.calen.drive.domain.DriveAdminConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DriveAdminConfigRepository extends JpaRepository<DriveAdminConfig, Long> {
}
