{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:e2e:install": "npm exec --yes --package @playwright/test -- playwright install chromium",
    "test:e2e:smoke": "npm exec --yes --package @playwright/test -- playwright test --config playwright.config.js",
    "test:e2e:smoke:headed": "npm exec --yes --package @playwright/test -- playwright test --config playwright.config.js --headed"
  },
  "dependencies": {
    "exifr": "^7.1.3",
    "gridstack": "^12.6.0",
    "leaflet": "^1.9.4",
    "pinia": "^3.0.4",
    "vue": "^3.5.25"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.2",
    "vite": "^7.3.1"
  }
}
